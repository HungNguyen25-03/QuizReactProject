import React from "react";

export default function Score({ text, classContent, handleClick }) {
  const mystyle = {
    width: "50%",
    height: "50%",
    borderRadius: "50%",
  };
  return <div style={mystyle}>Style</div>;
}
